subprojects {
    group = 'org.groovymc.modsdotgroovy.frontend-dsl'

    dependencies {
        implementation libs.groovy4.get()
        implementation projects.core
        compileOnly 'org.jetbrains:annotations:24.0.1'
    }
}

tasks.named('jar', Jar).configure {
    enabled = false
}
