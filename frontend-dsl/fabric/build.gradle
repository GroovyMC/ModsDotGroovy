groovydoc {
    source = sourceSets.main.allSource
    docTitle = "ModsDotGroovy v${project.version} - Frontend Fabric DSL"
}

publishing {
    publications {
        register('frontendDslFabric', MavenPublication) {
            artifactId = 'dsl'
            version = project.version

            artifacts = [jar, sourcesJar, groovydocJar]
        }
    }
}
